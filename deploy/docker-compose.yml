version: "3.9" # optional since v1.27.0

# docker build -t beefy-data-importer -f ./deploy/import/Dockerfile ./
# docker volume create --driver local --opt type=none --opt device=${PWD}/data/git-work --opt o=bind beefy_git_work_directory
# docker volume create --driver local --opt type=none --opt device=${PWD}/data/db/timescaledb --opt o=bind beefy_timescaledb_data_directory
# docker volume create --driver local --opt type=none --opt device=${PWD}/data/db/grafana --opt o=bind beefy_grafana_data_directory
# docker volume create --driver local --opt type=none --opt device=${PWD}/data/db/grafana_plugins --opt o=bind beefy_grafana_plugins_directory

# tune the system: https://docs.timescale.com/timescaledb/latest/how-to-guides/configuration/timescaledb-tune/#timescaledb-tuning-tool
# docker exec -it deploy-timescaledb-1 timescaledb-tune --yes --memory="20GB" --cpus=10 --dry-run

# docker compose -f deploy/docker-compose.yml up -d
# docker compose -f deploy/docker-compose.yml logs -f
# docker compose -f deploy/docker-compose.yml down
# docker compose -f deploy/docker-compose.yml rm # cleanup logs sometimes

# docker compose -f deploy/docker-compose.yml logs -f importer

# export TASK_LIST="importer-products importer-ignore-addresses importer-investor-cache importer-prices-historical importer-prices-recent importer-investment-recent-arbitrum importer-investment-recent-aurora importer-investment-recent-avax importer-investment-recent-bsc importer-investment-recent-canto importer-investment-recent-celo importer-investment-recent-cronos importer-investment-recent-ethereum importer-investment-recent-emerald importer-investment-recent-fantom importer-investment-recent-fuse importer-investment-recent-harmony importer-investment-recent-heco importer-investment-recent-kava importer-investment-recent-metis importer-investment-recent-moonbeam importer-investment-recent-moonriver importer-investment-recent-optimism importer-investment-recent-polygon importer-investment-recent-syscoin importer-investment-historical-arbitrum importer-investment-historical-aurora importer-investment-historical-avax importer-investment-historical-bsc importer-investment-historical-canto importer-investment-historical-celo importer-investment-historical-cronos importer-investment-historical-ethereum importer-investment-historical-emerald importer-investment-historical-fantom importer-investment-historical-fuse importer-investment-historical-harmony importer-investment-historical-heco importer-investment-historical-kava importer-investment-historical-metis importer-investment-historical-moonbeam importer-investment-historical-moonriver importer-investment-historical-optimism importer-investment-historical-polygon importer-investment-historical-syscoin importer-investment-historical-share-rate-arbitrum importer-investment-historical-share-rate-aurora importer-investment-historical-share-rate-avax importer-investment-historical-share-rate-bsc importer-investment-historical-share-rate-canto importer-investment-historical-share-rate-celo importer-investment-historical-share-rate-cronos importer-investment-historical-share-rate-ethereum importer-investment-historical-share-rate-emerald importer-investment-historical-share-rate-fantom importer-investment-historical-share-rate-fuse importer-investment-historical-share-rate-harmony importer-investment-historical-share-rate-heco importer-investment-historical-share-rate-kava importer-investment-historical-share-rate-metis importer-investment-historical-share-rate-moonbeam importer-investment-historical-share-rate-moonriver importer-investment-historical-share-rate-optimism importer-investment-historical-share-rate-polygon importer-investment-historical-share-rate-syscoin"
# docker build -t beefy-data-importer -f ./deploy/import/Dockerfile ./
# docker compose -f deploy/docker-compose.yml stop $TASK_LIST
# docker compose -f deploy/docker-compose.yml up $TASK_LIST -d --force-recreate
# docker compose -f deploy/docker-compose.yml logs -f $TASK_LIST
# docker compose -f deploy/docker-compose.yml logs -f $TASK_LIST | grep -vi 'archive node' | grep -vi 'missing revert data in call' | grep -vi 'missing trie node' | grep -vi 'The operation was unable to achieve a quorum during its retry window' | grep -vi 'Connection timeout error, will retry'

# docker compose -f deploy/docker-compose.yml up public-api -d --force-recreate
# RUN a command
# docker exec -it -e LOG_LEVEL=trace deploy-importer-investment-historical-1 node ./dist/src/script/run.js beefy:run -i historical -c kava
# docker exec -it -e LOG_LEVEL=trace deploy-importer-products-1 node ./dist/src/script/run.js db:migrate

x-importer: &importer
  image: beefy-data-importer
  restart: always
  env_file: ../.env
  environment:
    WORK_CMD: "./dist/src/script/run.js beefy:run -t products -l 2hour"
  stop_signal: SIGINT
  volumes:
    - git_work_directory:/git_work
  logging:
    driver: "local"
    options:
      max-size: "5M"
      max-file: "3"
  networks:
    - backend
  depends_on:
    - redis
    - timescaledb

x-importer-env: &importer-env
  REDIS_URL: redis://redis:6379
  TIMESCALEDB_URL: postgres://beefy:beefy@timescaledb:5432/beefy
  DATA_DIRECTORY: /data
  GIT_WORK_DIRECTORY: /git_work

volumes:
  git_work_directory:
    external: true
    name: beefy_git_work_directory
  timescaledb_data:
    external: true
    name: beefy_timescaledb_data_directory
  grafana_data:
    external: true
    name: beefy_grafana_data_directory
  grafana_plugins:
    external: true
    name: beefy_grafana_plugins_directory

networks:
  backend: {}
  proxy-exposed:
    name: proxy-exposed
    external: true

services:
  redis:
    image: redis:7-alpine
    restart: always
    command: redis-server --save "" --appendonly no --replicaof no one
    stop_signal: SIGINT
    networks:
      - backend

  timescaledb:
    image: timescale/timescaledb-ha:pg14.6-ts2.9.1-latest
    volumes:
      - timescaledb_data:/home/postgres/pgdata/data
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql
    command: postgres -c 'max_connections=250' -c logging_collector=off -c log_min_duration_statement=60s -c log_statement=none -c shared_buffers=16GB -c effective_cache_size=40GB -c timescaledb.max_background_workers=10 -c max_worker_processes=12 -c max_parallel_workers_per_gather=5 -c max_parallel_workers=10 -c max_parallel_maintenance_workers=4 -c maintenance_work_mem=10GB -c min_wal_size=4GB -c max_wal_size=16GB -c max_locks_per_transaction=500
    stop_signal: SIGINT
    shm_size: 256M
    deploy:
      resources:
        limits:
          cpus: "0.70"
          memory: 50G
        reservations:
          cpus: "0.25"
          memory: 15G
    logging:
      driver: "local"
      options:
        max-size: "5M"
        max-file: "3"
    environment:
      TZ: "UTC"
      POSTGRES_PORT: 5432
      POSTGRES_USER: beefy
      POSTGRES_PASSWORD: beefy
      POSTGRES_DB: beefy
      PGDATA: /home/postgres/pgdata/data
    networks:
      - backend

  grafana:
    image: grafana/grafana:9.2.15
    expose:
      - "3000"
    deploy:
      resources:
        limits:
          cpus: "0.20"
          memory: 1G
        reservations:
          cpus: "0.05"
          memory: 250M
    stop_signal: SIGINT
    environment:
      TZ: UTC
      POSTGRES_HOST: timescaledb
      POSTGRES_PORT: 5432
      POSTGRES_USER: grafana_ro
      POSTGRES_PASSWORD: grafana_ro
      POSTGRES_DB: beefy
      GF_INSTALL_PLUGINS: marcusolsson-json-datasource
      LETSENCRYPT_HOST: dm-control.beefy.com
      VIRTUAL_HOST: dm-control.beefy.com
      VIRTUAL_PORT: 3000
      TRUST_DOWNSTREAM_PROXY: true
    logging:
      driver: "local"
      options:
        max-size: "5M"
        max-file: "3"
    volumes:
      - grafana_data:/var/lib/grafana
      - grafana_plugins:/var/lib/grafana/plugins
      - ./db/grafana.ini:/etc/grafana/grafana.ini
      - ./db/grafana_datasource.yml:/etc/grafana/provisioning/datasources/timescaledb.yml
    depends_on:
      - timescaledb
    networks:
      - backend
      - proxy-exposed

  public-api:
    image: beefy-data-importer
    restart: always
    env_file: ../.env
    expose:
      - "8080"
    stop_signal: SIGINT
    environment:
      TZ: UTC
      REDIS_URL: redis://redis:6379
      TIMESCALEDB_URL: postgres://api_ro:api_ro@timescaledb:5432/beefy
      API_LISTEN: "::" # listen on all interfaces inside the container
      WORK_CMD: "./dist/src/api/server.js"
      LETSENCRYPT_HOST: databarn.beefy.com
      VIRTUAL_HOST: databarn.beefy.com
      VIRTUAL_PORT: 8080
      TRUST_DOWNSTREAM_PROXY: true
    logging:
      driver: "local"
      options:
        max-size: "5M"
        max-file: "3"
    networks:
      - backend
      - proxy-exposed
    depends_on:
      - redis
      - timescaledb

  importer-products:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -t products -l 2hour"

  importer-ignore-addresses:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -t ignore-address -l 1day"

  importer-investor-cache:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -t investor-cache -l 5min"

  importer-prices-historical:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -t historical-prices -c all -e true -S live -l 1hour"

  importer-prices-recent:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -t recent-prices -c all -l 15min"

  #importer-reward-snapshots:
  #  <<: *importer
  #  environment:
  #    <<: *importer-env
  #    WORK_CMD: "./dist/src/script/run.js beefy:run -t reward-snapshots -c all -l 4hour"

  # arbitrum aurora avax bsc canto celo cronos ethereum emerald fantom fuse harmony heco kava metis moonbeam moonriver optimism polygon syscoin

  importer-investment-recent-arbitrum:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -t recent -c arbitrum -l 15sec"

  importer-investment-recent-aurora:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -t recent -c aurora -l 15sec"

  importer-investment-recent-avax:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -t recent -c avax -l 15sec"

  importer-investment-recent-bsc:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -t recent -c bsc -l 15sec -e" # make ankr handle recent blocks to protect nodereal rpc when it's doing historical

  importer-investment-recent-canto:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -t recent -c canto -l 15sec"

  importer-investment-recent-celo:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -t recent -c celo -l 15sec"

  importer-investment-recent-cronos:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -t recent -c cronos -l 15sec"

  importer-investment-recent-ethereum:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -t recent -c ethereum -l 15sec"

  importer-investment-recent-emerald:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -t recent -c emerald -l 15sec"

  importer-investment-recent-fantom:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -t recent -c fantom -l 15sec"

  importer-investment-recent-fuse:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -t recent -c fuse -l 15sec"

  importer-investment-recent-harmony:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -t recent -c harmony -l 15sec"

  importer-investment-recent-heco:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -t recent -c heco -l 15sec"

  importer-investment-recent-kava:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -t recent -c kava -l 15sec"

  importer-investment-recent-metis:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -t recent -c metis -l 15sec"

  importer-investment-recent-moonbeam:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -t recent -c moonbeam -l 15sec"

  importer-investment-recent-moonriver:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -t recent -c moonriver -l 15sec"

  importer-investment-recent-optimism:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -t recent -c optimism -l 15sec"

  importer-investment-recent-polygon:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -t recent -c polygon -l 15sec"

  importer-investment-recent-syscoin:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -t recent -c syscoin -l 15sec"

  # arbitrum aurora avax bsc canto celo cronos ethereum emerald fantom fuse harmony heco kava metis moonbeam moonriver optimism polygon syscoin

  importer-investment-historical-arbitrum:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical -c arbitrum -l 15min -S live"

  importer-investment-historical-aurora:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical -c aurora -l 15sec -S live"

  importer-investment-historical-avax:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical -c avax -l 15sec -S live"

  importer-investment-historical-bsc:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical -c bsc -l 15min -S all" # recent import is also importing eol, so we can exclude recent range totally

  importer-investment-historical-canto:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical -c canto -l 15sec -S live"

  importer-investment-historical-celo:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical -c celo -l 15sec -S live"

  importer-investment-historical-cronos:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical -c cronos -l 15sec -S live"

  importer-investment-historical-ethereum:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical -c ethereum -l 15sec -S live"

  importer-investment-historical-emerald:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical -c emerald -l 15sec -S live"

  importer-investment-historical-fantom:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical -c fantom -l 15sec -S live"

  importer-investment-historical-fuse:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical -c fuse -l 15sec -S live"

  importer-investment-historical-harmony:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical -c harmony -l 15sec -S live"

  importer-investment-historical-heco:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical -c heco -l 15sec -S live"

  importer-investment-historical-kava:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical -c kava -l 15sec -S live"

  importer-investment-historical-metis:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical -c metis -l 15sec -S live"

  importer-investment-historical-moonbeam:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical -c moonbeam -l 15sec -S live"

  importer-investment-historical-moonriver:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical -c moonriver -l 15sec -S live"

  importer-investment-historical-optimism:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical -c optimism -l 15min -S live"

  importer-investment-historical-polygon:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical -c polygon -l 15sec -S live"

  importer-investment-historical-syscoin:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical -c syscoin -l 15sec -S live"

  # arbitrum aurora avax bsc canto celo cronos ethereum emerald fantom fuse harmony heco kava metis moonbeam moonriver optimism polygon syscoin

  importer-investment-historical-share-rate-arbitrum:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical-share-rate -c arbitrum -l 15min -S none"

  importer-investment-historical-share-rate-aurora:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical-share-rate -c aurora -l 15min -S none"

  importer-investment-historical-share-rate-avax:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical-share-rate -c avax -l 15min -S none"

  importer-investment-historical-share-rate-canto:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical-share-rate -c canto -l 15min -S none"

  importer-investment-historical-share-rate-bsc:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical-share-rate -c bsc -l 15min -S none"

  importer-investment-historical-share-rate-celo:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical-share-rate -c celo -l 15min -S none"

  importer-investment-historical-share-rate-cronos:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical-share-rate -c cronos -l 15min -S none"

  importer-investment-historical-share-rate-ethereum:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical-share-rate -c ethereum -l 15min -S none"

  importer-investment-historical-share-rate-emerald:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical-share-rate -c emerald -l 15min -S none"

  importer-investment-historical-share-rate-fantom:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical-share-rate -c fantom -l 15min -S none"

  importer-investment-historical-share-rate-fuse:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical-share-rate -c fuse -l 15min -S none"

  importer-investment-historical-share-rate-harmony:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical-share-rate -c harmony -l 15min -S none"

  importer-investment-historical-share-rate-heco:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical-share-rate -c heco -l 15min -S none"

  importer-investment-historical-share-rate-kava:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical-share-rate -c kava -l 15min -S none"

  importer-investment-historical-share-rate-metis:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical-share-rate -c metis -l 15min -S none"

  importer-investment-historical-share-rate-moonbeam:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical-share-rate -c moonbeam -l 15min -S none"

  importer-investment-historical-share-rate-moonriver:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical-share-rate -c moonriver -l 15min -S none"

  importer-investment-historical-share-rate-optimism:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical-share-rate -c optimism -l 15min -S none"

  importer-investment-historical-share-rate-polygon:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical-share-rate -c polygon -l 15min -S none"

  importer-investment-historical-share-rate-syscoin:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t historical-share-rate -c syscoin -l 15min -S none"

  # arbitrum aurora avax bsc canto celo cronos ethereum emerald fantom fuse harmony heco kava metis moonbeam moonriver optimism polygon syscoin

  importer-investment-recent-share-rate-arbitrum:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t recent-share-rate -c arbitrum -l 15min -S none"

  importer-investment-recent-share-rate-aurora:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t recent-share-rate -c aurora -l 15min -S none"

  importer-investment-recent-share-rate-avax:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t recent-share-rate -c avax -l 15min -S none"

  importer-investment-recent-share-rate-canto:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t recent-share-rate -c canto -l 15min -S none"

  importer-investment-recent-share-rate-bsc:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t recent-share-rate -c bsc -l 15min -S none"

  importer-investment-recent-share-rate-celo:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t recent-share-rate -c celo -l 15min -S none"

  importer-investment-recent-share-rate-cronos:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t recent-share-rate -c cronos -l 15min -S none"

  importer-investment-recent-share-rate-ethereum:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t recent-share-rate -c ethereum -l 15min -S none"

  importer-investment-recent-share-rate-emerald:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t recent-share-rate -c emerald -l 15min -S none"

  importer-investment-recent-share-rate-fantom:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t recent-share-rate -c fantom -l 15min -S none"

  importer-investment-recent-share-rate-fuse:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t recent-share-rate -c fuse -l 15min -S none"

  importer-investment-recent-share-rate-harmony:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t recent-share-rate -c harmony -l 15min -S none"

  importer-investment-recent-share-rate-heco:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t recent-share-rate -c heco -l 15min -S none"

  importer-investment-recent-share-rate-kava:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t recent-share-rate -c kava -l 15min -S none"

  importer-investment-recent-share-rate-metis:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t recent-share-rate -c metis -l 15min -S none"

  importer-investment-recent-share-rate-moonbeam:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t recent-share-rate -c moonbeam -l 15min -S none"

  importer-investment-recent-share-rate-moonriver:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t recent-share-rate -c moonriver -l 15min -S none"

  importer-investment-recent-share-rate-optimism:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t recent-share-rate -c optimism -l 15min -S none"

  importer-investment-recent-share-rate-polygon:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t recent-share-rate -c polygon -l 15min -S none"

  importer-investment-recent-share-rate-syscoin:
    <<: *importer
    environment:
      <<: *importer-env
      WORK_CMD: "./dist/src/script/run.js beefy:run -e true -t recent-share-rate -c syscoin -l 15min -S none"
